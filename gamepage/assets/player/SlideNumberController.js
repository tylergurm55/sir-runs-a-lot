const SlideNumberController = Class.create({
  initialize(a, b) { this.domNode = a; this.width = 120; this.height = 110; this.slideNumberLabel = document.createElement('div'); this.slideNumberLabel.setAttribute('class', 'slideNumberLabel'); if (b) { this.slideNumberLabel.innerHTML = b; } else { this.slideNumberLabel.innerHTML = 'Press Return to go to slide:'; } this.slideNumberDigit = document.createElement('div'); this.slideNumberDigit.setAttribute('class', 'slideNumberDigit'); this.domNode.appendChild(this.slideNumberLabel); this.domNode.appendChild(this.slideNumberDigit); this.isShowing = false; }, setPosition(b, a) { this.domNode.style.left = `${b}px`; this.domNode.style.top = `${a}px`; }, setSlideNumber(a) { this.slideNumberDigit.innerHTML = a; }, show() { this.isShowing = true; this.domNode.style.display = 'block'; this.domNode.style.opacity = 1; }, hide() { this.isShowing = false; this.domNode.style.display = 'none'; this.domNode.style.opacity = 0; },
}); const SlideNumberDisplay = Class.create({
  initialize(a) { this.domNode = a; this.width = 100; this.height = 100; this.slideNumberDigit = document.createElement('div'); this.slideNumberDigit.setAttribute('class', 'slideNumberDisplayDigit'); this.domNode.appendChild(this.slideNumberDigit); this.isShowing = false; }, setPosition(b, a) { this.domNode.style.left = `${b}px`; this.domNode.style.top = `${a}px`; }, setSlideNumber(a) { this.slideNumberDigit.innerHTML = a; }, show(a) { this.isShowing = true; this.domNode.style.display = 'block'; this.domNode.style.opacity = 1; }, hide() { this.isShowing = false; this.domNode.style.display = 'none'; this.domNode.style.opacity = 0; },
});
